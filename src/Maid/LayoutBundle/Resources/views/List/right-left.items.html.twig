<div class="container items">
    {% for item in items %}
        {{ include('MaidLayoutBundle:List:right-left.item.html.twig', {'item': item, 'index': loop.index}) }}
        <hr/>
    {% endfor %}
</div> 